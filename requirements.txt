# AI AURA runtime dependencies (Python 3.11+; developed on 3.13)
# Vendored pocketoptionapi (services/market_data/vendor) needs: websockets,
# pandas, tzlocal, backoff, requests.
websockets>=12.0
pandas>=2.2.3
tzlocal>=5.2
backoff>=2.2.1
requests>=2.32.0
python-dotenv>=1.0.1
pyarrow>=17.0.0

# API + PWA host (apps/api)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
psycopg[binary]>=3.2.0   # Postgres/Supabase store (cloud); SQLite needs no driver

# Dev / test
pytest>=8.0.0
Pillow>=10.0.0        # scripts/make_icons.py (PWA icon generation, one-off)
